import { Placeholder } from '@/components/placeholder';

export default function SignIn() {
  return (
    <Placeholder title="Sign in with Google" phase="Phase 1">
      Any email is accepted — Gmail included. A recognised college domain skips the
      ID step entirely and lands the user at Tier 2. Needs a Google Cloud OAuth client.
    </Placeholder>
  );
}
