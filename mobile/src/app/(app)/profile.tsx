import { Placeholder } from '@/components/placeholder';

export default function Profile() {
  return (
    <Placeholder title="You" phase="Phase 1">
      Display picture, name, college and tier badge. Alumni show a badge once their
      course period ends — they keep full access.
    </Placeholder>
  );
}
