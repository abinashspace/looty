import { Placeholder } from '@/components/placeholder';

export default function College() {
  return (
    <Placeholder title="Find your college" phase="Phase 1">
      Searchable list, or request one that is missing. Only shown on the ID path —
      college email users already have theirs assigned from the domain.
    </Placeholder>
  );
}
