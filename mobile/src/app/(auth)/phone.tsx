import { Placeholder } from '@/components/placeholder';

export default function Phone() {
  return (
    <Placeholder title="Verify your number" phase="Phase 1">
      The phone number is the ban anchor — a new Gmail takes two minutes, an Indian
      SIM does not. Stored hashed, never raw. Needs an SMS provider, and Indian
      transactional SMS requires DLT registration.
    </Placeholder>
  );
}
